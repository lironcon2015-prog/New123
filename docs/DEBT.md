# חוב טכני

אפס `TODO` / `FIXME` / `HACK` בקוד (§1). כל חוב נרשם כאן.

פורמט רשומה:

```
## D<n> — <כותרת>
**נוצר:** <תאריך> · **שלב:** <מספר>
**מה:** מה בדיוק חלקי או לא נכון
**למה נדחה:** מה הצדיק את הדחייה עכשיו
**מה שובר את זה:** התרחיש שבו החוב הופך לבאג
**תיקון:** מה צריך לקרות כדי לסגור
```

---

## D1 — אין העתקה של הצורה השנייה
**נוצר:** 2026-08-22 · **שלב:** 2
**מה:** כל `kind` מעתיק צורה אחת בלבד — קנונית או מוצגת (`SPEC.md` §4). אין דרך להעתיק תאריך כ-`2026-11-01` או לוחית רישוי עם מקפים.
**למה נדחה:** נגיעה אחת = פעולה אחת. תפריט בחירה על שורת שדה הורס את האינטראקציה שכל האפליקציה בנויה סביבה.
**מה שובר את זה:** טופס שדורש `DD/MM/YYYY` בשדה שהוגדר קנוני, או להפך. אז מקלידים ידנית.
**תיקון:** לחיצה ארוכה על שורה שה-`kind` שלה מגדיר שתי צורות → העתקת הצורה השנייה עם toast שאומר איזו. דורש שה-`kind` יצהיר `altCopy`.

## D2 — הפרשי שעון בין מכשירים משפיעים על המיזוג
**נוצר:** 2026-08-22 · **שלב:** 2
**מה:** המיזוג בשלב 5 מכריע לפי `updatedAt`, שנוצר משעון המכשיר. מכשיר עם שעון מוקדם בדקה יפסיד עריכות שנעשו בו אחרי המכשיר השני.
**למה נדחה:** אין backend ואין שעון סמכותי. שני משתמשים בבית אחד, והסבירות לעריכה מקבילה של אותה רשומה נמוכה.
**מה שובר את זה:** מכשיר שהשעון בו סטה בדקות, ועריכה של אותו מסמך בשני המכשירים באותו יום.
**תיקון:** מונה גרסה לכל רשומה במקום חותמת זמן, או וקטור שעונים. שינוי סכמה, לא שינוי לוגיקה. ממתין לתשובת `ASK-NAVIGO.md` Q4 לפני שנוגעים.

## D3 — חיפוש בסריקה ליניארית
**נוצר:** 2026-08-22 · **שלב:** 2
**מה:** מסך ההעתקה המהירה סורק את כל השדות של כל המסמכים בכל הקלדה (`SPEC.md` §8). אין אינדקס.
**למה נדחה:** מאה מסמכים = 600 מחרוזות. אינדקס הפוך הוא מבנה נתונים שצריך לתחזק, לסנכרן ולנפות, בתמורה לחיסכון שאינו נמדד.
**מה שובר את זה:** מעל אלף מסמכים, או מכשיר איטי שבו ההקלדה מתחילה להיתקע. זה הסף למדוד.
**תיקון:** אינדקס בזיכרון שנבנה פעם אחת בטעינה ומתעדכן בשמירה. לא אינדקס ב-IndexedDB.

## D4 — מסלול HEIC לא נבדק על קובץ אמיתי
**נוצר:** 2026-08-22 · **שלב:** 3
**מה:** הנרמול נשען על `createImageBitmap` כדי לפענח HEIC, וזה עובד ב-Safari בלבד. סביבת הבדיקה היא Chromium, שאינו מפענח HEIC כלל, ואין בה קובץ HEIC אמיתי.
**למה נדחה:** אי אפשר לבדוק את זה כאן. מסלול הכישלון כן נבדק — קובץ שאינו ניתן לפענוח נדחה עם הודעה בעברית ואינו נשמר שבור.
**מה שובר את זה:** צילום מ-iPhone שנשמר כ-HEIC, במקרה שבו `createImageBitmap` מתנהג אחרת ממה שהנחתי.
**תיקון:** בדיקה ידנית אחת על iPhone אמיתי בסוף שלב 3. אם היא נכשלת — נדרש מפענח HEIC בצד לקוח, וזו החלטה חדשה.

## D5 — נכסי ה-OCR ומשקל ה-precache · **נסגר 2026-08-22**
**איך נסגר:** נמדד. המודל `4.0.0_best_int` (2.9MB) קורא את אותו MRZ באותה תוצאה בדיוק וגם פי שניים מהר מהמודל השלם (10.4MB), ולכן ההורדה החד-פעמית עומדת על **6.7MB במקום 14.3MB**. הכל מתארח עצמית ב-`lib/tesseract/`, נטען עצלה, ורק גרסת הגרעין שהדפדפן בוחר נמשכת בפועל. המסלול הוא כפתור נפרד עם המשקל כתוב עליו. מה שנשאר לשלב 6: להחליט אם הנכסים נכנסים ל-precache או נשארים ב-runtime cache.

_הרשומה המקורית:_

**נוצר:** 2026-08-22 · **שלב:** 4
**מה:** נאביגו טוענת את Tesseract מ-`cdn.jsdelivr.net`, ואת `eng.traineddata` מברירת המחדל של הספרייה — הוסט שלישי שאינו ברשימת הקאשינג. **קריאת MRZ ראשונה אצלה חייבת רשת**, וההערה בראש הקובץ שלה ("assets cached by the SW") אינה מדויקת. אצלנו אסור להעתיק את זה: האפליקציה מבטיחה אופליין.
**למה נדחה:** ההחלטה עצמה סגורה — אירוח עצמי same-origin. מה שנדחה הוא **המשקל**: `eng.traineddata` הוא עשרות מגה-בייט, ו-precache שלו הופך התקנה ראשונה לכבדה מאוד.
**מה שובר את זה:** סריקת דרכון ראשונה בלי רשת. או, בכיוון השני, התקנה ראשונה על חבילת גלישה.
**תיקון:** למדוד את הקובץ בפועל בשלב 4 ואז להכריע: precache מלא, או precache ל-JS ול-worker בלבד עם fetch חד-פעמי של ה-traineddata שנתפס ב-runtime cache. אם המשקל בלתי אפשרי — לבדוק תת-סט מאומן ל-`A-Z0-9<` בלבד, שזה כל מה ש-MRZ צריך.

**הוכרע בשלב 6:** Tesseract **אינו** ב-precache. 6.7MB ברשימת ההתקנה עבור יכולת שרוב
המשתמשים לא יגעו בה היא עסקה גרועה. הוא נתפס ב-runtime cache בסריקה הראשונה, ומאז
עובד אופליין. הכפתור בגיליון ההוספה אומר את המשקל מראש.

## D6 — הצופה עדיין על iframe · **נסגר 2026-08-23**
**איך נסגר:** pdf.js מתארח עצמית ומרנדר ל-`<canvas>`, כולל `ctx.direction = 'ltr'` ו-precache מפורש של 14 גופני התחליף. יש בדיקה שמרנדרת PDF אמיתי **אופליין** ומוודאת שיש דיו על הקנבס, ובדיקה נוספת שכל `blob:` URL מבוטל בסגירה.

_הרשומה המקורית:_

**נוצר:** 2026-08-22 · **שלב:** 3
**מה:** `UI.viewer` מציג PDF ב-`<iframe>` עם `blob:` URL. נאביגו למדה בכאב ש-`blob:` ב-iframe אינו אמין ב-iOS Safari, ועברה ל-pdf.js על `<canvas>` self-hosted.
**למה נדחה:** pdf.js הוא כמה מגה-בייט של נכסים שערכם המלא מתממש רק עם Service Worker, ולכן זה שייך לשלב 6.
**מה שובר את זה:** פתיחת PDF ב-iPhone. יש מסלול נסיגה — "פתיחה בכרטיסייה חדשה" — אבל הוא יוצא מהאפליקציה.
**תיקון:** לפורט את `renderPdf` משלב 6, **כולל שתי השורות שנאביגו סימנה במפורש**: `ctx.direction = 'ltr'` (בלעדיה `fillText` מתעגן הפוך באפליקציית RTL וה-clip של ה-PDF חותך אותיות — הסימפטום נראה כמו PDF פגום), ו-precache מפורש של `standard_fonts/` (הם נטענים עצלה ולכן runtime caching לא תופס אותם).

## D7 — הפרשי שעון, אושרר
**נוצר:** 2026-08-22 · **שלב:** 2 · **מעדכן את D2**
**מה:** נאביגו אישרה במפורש: אין שרת זמן, אין Lamport clock, אין hybrid timestamp. מכשיר עם שעון מקדים ינצח כל התנגשות עד שהצד השני יערוך שוב.
**למה נדחה:** אין backend. שני משתמשים בבית אחד.
**תיקון:** ראה D2. נשאר חוב מוכר ומשותף לשתי האפליקציות.

## D8 — סבב אמיתי מול גוגל לא נבדק
**נוצר:** 2026-08-22 · **שלב:** 5
**מה:** `js/drive.js` נבדק מול Google API מזויף — 29 בדיקות שמכסות איתור תיקייה, שחזור דרך `appDataFolder`, בניית `multipart/related`, ומסלולי כשל. **אף בקשה אמיתית לגוגל לא בוצעה**, כי אין חשבון בסביבת הפיתוח.
**למה נדחה:** אי אפשר לבדוק את זה כאן. המיזוג עצמו — החלק המסוכן — כן נבדק מול שני דפדפנים אמיתיים.
**מה שובר את זה:** הבדלים בין ה-API המזויף לאמיתי: התנהגות `spaces=appDataFolder`, מגבלות גודל, קודי שגיאה שלא צפיתי, או פרטי ה-GIS token client שאין דרך לדמות.
**תיקון:** חיבור ראשון אמיתי מול חשבון גוגל, עם מזהה OAuth אמיתי. עד אז, הכפתור בהגדרות עלול להיכשל בדרכים שלא ניפיתי.


## D9 — התקנה כאפליקציה לא נבדקה על מכשיר
**נוצר:** 2026-08-23 · **שלב:** 6
**מה:** ה-Service Worker, המניפסט והאייקונים נבדקים ב-Chromium headless — רישום, precache, וטעינה מלאה אופליין. **התקנה אמיתית לא נבדקה**: לא "הוסף למסך הבית" ב-iOS, לא באנר ההתקנה באנדרואיד, ולא התנהגות ה-`display: standalone` בפועל.
**למה נדחה:** אין מכשיר בסביבה.
**מה שובר את זה:** iOS מתעלם מחלק מהמניפסט ונשען על תגי `apple-*` (הוספו), ומגביל אחסון ל-PWA שלא נפתחה זמן מה. אנדרואיד דורש קריטריונים להצגת באנר ההתקנה שלא בדקתי.
**תיקון:** התקנה ידנית אחת על iPhone ואחת על אנדרואיד. יחד עם D4 (HEIC) ו-D8 (דרייב אמיתי) — אלה שלושת הדברים שדורשים מכשיר אמיתי.
