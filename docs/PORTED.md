# קוד מיובא מנאביגו

כל קטע שמקורו בנאביגו מסומן בראשו `// ported from Navigo: <קובץ>:<פונקציה>` ונרשם כאן.

| תאריך | יעד | מקור בנאביגו | מה שונה |
|---|---|---|---|
| — | — | — | — |

_אין עדיין קוד מיובא._ שלוש נקודות ממתינות לתשובות מ-`ASK-NAVIGO.md`, וכולן מסומנות בקוד `// STUB pending Q<n>`:

| stub | קובץ | שאלה | מה קיים בינתיים |
|---|---|---|---|
| Q6 | `js/vault.js` | שער PIN | SHA-256 ללא מלח, hex, נעילה בטעינה ואחרי זמן ברקע. עובד מקצה לקצה. |
| Q8 | `js/ui.js` | צופה PDF | תמונות מטופלות במלואן. PDF ב-iframe עם blob URL ומסלול "פתיחה בכרטיסייה". אין זום ופאן. |
| Q9 | `js/util.js` | דפוס bidi | `U.bidi()` עוטף ב-`<bdi dir="ltr">` כל ערך שיש בו ספרה או אות לטינית. נקודה אחת בקוד. |

כשתגיע תשובה, ההחלפה היא בקובץ אחד לכל stub, והשורה נרשמת בטבלה למעלה.
